//- player-info.pug
include ../partials/header.pug
include ../partials/menu.pug

div.container
  br
  div.row
    div.col-4
      div.card
        //- TODO: Modify the pug code below to conditionally set the image_src
        var images[] = {player1.jpg, player2.jpg, player3.jpg, player4.jpg, player5.jpg, player6.jpg, player7.jpg, player8.jpg, player9.jpg, player10.jpg}
        //- select number in array corresponding to selscted player
        var selected = 
        - let image_src = '../../resources/img/images[selected]'

        img.card-img-top#player_img(src= image_src, alt="Football Player")
        div.card-body.mx-auto
          form.form(action="/player_info/post", method="get")
            select.custom-select(name="player_choice", onchange="this.form.submit()")
              option(selected) Select Player
              //- TODO: Populate the select with player options. Set the value to the player's id and display the player's name.
              for i in result_1
                option(value=i.id)= i.name
    div.col-8
      div.table-responsive
        table.table
          //- TODO: Set the player's information. On select, fill in the player's information in each of the field below. You will need to calculate the average rushing, receiving, and passing yards.
          tr
            th Year
            td #(vals.year)
          tr
            th Major
            td #(vals.major)
          tr
            th Games Played
            td #
          tr
            td Passing Yards:
            var avg_passing = 0
            var sum1 = 0
            var avg_rushing = 0
            var sum2 = 0
            var avg_receiving = 0
            var sum3 = 0
            for vals in footballPlayer
              sum1 += avg_passing
              sum2 += avg_rushing
              sum3 += avg_receiving
              //-make a variable to count number of games played
              avg_passing = sum1/count
              avg_rushing = sum2/count
              avg_receiving = sum3/count
            td Avg. Passing Yards: #(avg_passing)
          tr
            td Rushing Yards: 
            td Avg. Rushing Yards: #(avg_rushing)
          tr
            td Receiving Yards:
            td Avg. Receiving Yards: #(avg_receiving)

include ../partials/footer.pug